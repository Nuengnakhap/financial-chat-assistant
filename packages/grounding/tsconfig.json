{
  // Editor, ESLint and `pnpm typecheck`. Covers tests, so type-aware rules reach them.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "composite": false,
    "declaration": false,
    "declarationMap": false
  },
  "include": ["src/**/*.ts"]
}
