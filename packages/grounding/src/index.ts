/** The public surface. A deep import couples the caller to a file layout free to change. */

export {
  add,
  compare,
  divide,
  exact,
  isInteger,
  isNegative,
  multiply,
  negate,
  ratio,
  roundToInteger,
  subtract,
  toApproximateNumber,
  type Quantity,
} from './quantity';
