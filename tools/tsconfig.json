{
  // Build tooling. Outside the solution build: nothing here is emitted.
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    // The local ESLint rules are .mjs with JSDoc types; their spec imports them.
    "allowJs": true,
    "composite": false,
    "declaration": false,
    "declarationMap": false,
    "module": "esnext",
    "moduleResolution": "bundler",
    "types": ["node"]
  },
  "include": ["**/*.ts"],
  "exclude": ["architecture/fixtures/**"]
}
